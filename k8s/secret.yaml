# EXAMPLE ONLY — do not commit real values.
# Apply manually: kubectl apply -f k8s/secret.yaml
# Or use sealed-secrets / external-secrets for production.
apiVersion: v1
kind: Secret
metadata:
  name: kana-secret
  namespace: kana
type: Opaque
stringData:
  DB_PATH: /data/kana.db
